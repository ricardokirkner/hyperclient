============
Installation
============

At the command line::

    $ easy_install hyperclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv hyperclient
    $ pip install hyperclient